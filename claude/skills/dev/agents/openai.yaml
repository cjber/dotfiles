interface:
  display_name: "Dev main"
  short_description: "Run the shared main development stack"
  default_prompt: "Start and verify the scoped development services from each repository main checkout."
